RSpec.describe App::Store::Connect do
  it "has a version number" do
    expect(App::Store::Connect::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
